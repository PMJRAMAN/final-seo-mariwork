# Second Review — Start Here

Authoritative inputs are the 167 page dossiers under `pages/`, the sitewide audits under `audits/sitewide/`, `registry/URL-INVENTORY.csv`, and the deterministic indexes in this directory.
The handoff contains 167 entities and 324 existing findings; repeated candidates are in `round1-repeated-findings.csv`.
Review sitewide findings first, then Store/shop, then the remaining groups in `round1-review-index.csv` order.
Round-1 findings are Codex findings awaiting independent Second Review; `final_disposition` remains `NOT_DECIDED` until human approval.
GSC `Pages.csv` and `Queries.csv` are separate aggregations and must not be treated as page-query joined data.
This handoff adds no SEO analysis, recommendations, or Production changes.
